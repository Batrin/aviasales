const initialState = {
  activeTabName: 'Самый дешевый',
  activeFilterObj: {
    all: false,
    withoutTransfer: false,
    oneTransfer: false,
    twoTransfer: false,
    threeTransfer: false,
  },
};

export default initialState;
